# 운영환경에서는 RDS를 사용하므로 이 파일은 사용하지 않음
# 개발환경용 docker-compose.yml만 사용

# RDS 연결 정보:
# - Endpoint: GitHub Secrets의 DB_HOST 사용
# - Port: 3306
# - Database: GitHub Secrets의 DB_NAME 사용
# - Username/Password: GitHub Secrets에서 관리

version: '3.8'
# 운영환경에서는 사용하지 않음 - RDS로 대체됨
