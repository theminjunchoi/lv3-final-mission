-- 데이터베이스 초기화 스크립트
USE mj_dev;

-- 여기에 초기 테이블 생성이나 데이터 삽입 쿼리 추가
-- 예시:
-- CREATE TABLE IF NOT EXISTS users (
--     id BIGINT AUTO_INCREMENT PRIMARY KEY,
--     username VARCHAR(255) NOT NULL,
--     email VARCHAR(255) NOT NULL,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );
